---
name: REPORT_PPT
description: 한국표준협회(KSA) 모노톤 보고서용 PPT를 만드는 스킬. A4 가로·맑은 고딕·흑백 무채색으로, 표와 각주까지 담는 읽는 문서형 장표를 만든다. "보고서 PPT 만들어줘", "문서형 장표", "실행계획 보고서", "결과보고서 슬라이드", "정의서·명세서 장표", "표 중심 보고 자료", "제출용 보고서 덱"이라고 하면 이 스킬을 쓴다. 명시적으로 "보고서"라 하지 않아도 표·수치·각주가 많고 화면이 아니라 손에 들고 읽는 자료면 이 스킬을 우선 검토한다. 본문 9.5pt·행 0.335"의 고밀도 조판에 PowerPoint 네이티브 표를 기본으로 쓰고, 5존 고정 좌표(챕터 0.40"/헤드라인 1.00"/부제 1.63"/본문 2.20–6.85"/푸터 7.05")·무채색 팔레트·표지·목차·슬라이드 마스터 자동 번호를 자동 점검기로 강제한다. 내용이 많으면 dense 옵션으로 본문 9pt까지 낮춘다. 화면에 띄우는 발표 덱은 PRESENT_PPT 스킬 담당이며, 이 스킬은 읽는 문서 전용이다.
---

# 보고서용 PPT — KSA 모노톤

정체성은 한 줄로: **순백 A4 가로 위에 5개 존이 전 장표에서 흔들리지 않고, 색은 하나도 쓰지 않으며, 한 장에 근거까지 담는다.**

값(색·좌표·규칙)의 최종 권한은 **`references/design-system.md`**, 컴포넌트 사용법은 **`references/components.md`** 에 있다. 파일 조작 메커니즘은 **pptx 스킬**을 따른다.

## 0. 시작 전 확인

| 확인 | 조치 |
|---|---|
| **판형** | A4 가로(10.8333"×7.5")만. 16:9·4:3 요청은 거절하고 A4 가로로 제안 |
| **서체** | 맑은 고딕만. 대체 폰트 제안도 하지 않는다 |
| **색** | **무채색만.** 유채색을 넣지 않는다 |
| **로고** | `assets/ksa_logo.jpg`가 동봉돼 있고 자동으로 들어간다 |
| **데이터** | 근거 없는 수치를 지어내지 않는다. 표에는 출처·산식·각주를 붙인다 |
| **발표덱과의 구분** | 화면에 띄우고 말로 설명하는 자료라면 PRESENT_PPT를 쓴다 |

## 1. 이 스킬의 밀도

보고서용은 **작게 많이**다. 한 장에 표 1–2개와 각주, 12행 표가 들어간다.

| 모드 | 헤드라인 | 부제 | 본문 | 표 행 | 한 장 수용 | 최소 글자 |
|---|---|---|---|---|---|---|
| 기본 | 20pt | 11.5pt | 9.5pt | 0.335" | 11행 | 9pt |
| `dense` | 19pt | 11pt | 9pt | 0.295" | **13행** | 8.5pt |

본문은 **2.20"–6.85"** (4.65")다. 제목 블록과 본문 사이는 0.17"로 붙어 있고, **표는 하단 기준선 6.85"까지 채운다.**

**`dense`는 기본이 아니다.** 20행짜리 업무 목록처럼 기본 밀도로 한 장에 안 들어갈 때만 `createDeck({mode:'report', dense:true})`로 쓴다. 공간이 남는데 글자만 줄이면 여백이 늘어 빈약해진다. **8.5pt 아래로는 내려가지 않는다** — A4 100% 인쇄에서 한글 받침이 눌린다.

한 장에 들어가는 요소는 발표용의 약 2배(평균 80개)다. 그래도 **본문 벽 2.39"–6.85"는 지킨다.**

## 2. 만드는 법

```bash
SK=~/.claude/skills/REPORT_PPT
mkdir build && cp $SK/assets/{ksa_mono.js,example_deck.js,ksa_logo.jpg} build/ && cd build
node example_deck.js deck.pptx                      # example_deck.js를 복사해 내용만 교체
python3 $SK/scripts/postprocess.py deck.pptx        # 필수 — 아래 설명
python3 $SK/scripts/check_layout.py deck.pptx --mode report --special 1
```

**`postprocess.py`는 선택이 아니다.** pptxgenjs 4.x가 차트에 *선언하지도 않은 축 참조*를 써 넣어, 그대로 두면 PowerPoint가 **"내용에 문제가 있습니다 / 복구하시겠습니까"** 대화상자를 띄운다. LibreOffice는 조용히 무시하므로 렌더 QA로는 드러나지 않는다. 점검기가 이를 검사하므로 빠뜨리면 FAIL로 막힌다.

`pptxgenjs`는 pptx 스킬 환경에 이미 설치돼 있다(`npm install` 먼저 하지 말 것).

## 3. 표준 골격 (13장 예시)

표지 → 목차 → Executive Summary → 진단 개요 → 업무 구조 12행 표 → 절감 경로(워터폴+표 2개) → 요인 분해(트리+표) → 과제 정의서 → 게이트 기준 → 우선순위 평가(매트릭스+평가표) → R&R → 로드맵(간트+분기표) → 지표 정의서.

`assets/example_deck.js`가 이 13장을 그대로 만든다. 복사해 내용만 갈아끼운다.

**표지는 사진으로 만든다. 로고는 넣지 않는다.**

```js
B.cover(deck, { org:'○○공단', title:'AX 전환 실행계획\n(최종보고)',
  date:'2026.01', by:'한국표준협회',
  photos:['공연.jpg','전경.jpg','교실.jpg','축제.jpg','대표.jpg'] });   // 생략하면 자리표시
```

상단에 사진 4장을 사선으로 나눈 띠, 좌하단 먹 패널(제목·날짜·수행기관), 우하단 대형 사진 1장으로 짠다.
사진 5장은 **각각 독립된 그림 도형**이라 PowerPoint에서 [그림 바꾸기]로 갈아끼워도 사선이 그대로 유지된다.
`photos`를 주지 않으면 `assets/photo_placeholder.png` 자리표시가 들어가므로, **사진 없이 먼저 출고하고 나중에 끼워 넣어도 된다.**

사선은 `postprocess.py`가 띠 사진의 도형을 평행사변형으로 바꿔 만든다 — **표지를 쓰면 후처리를 반드시 돌린다.**
사진이 없거나 격자 도형 표지를 쓰고 싶으면 `B.coverPlain(deck, {...})`을 쓴다.

**사진은 고객사 제공본을 우선한다.** 웹에서 가져올 때는 공공누리 1유형 또는 CC0/CC-BY(출처 표기)만 쓰고,
출처가 불분명한 이미지는 제출용 문서에 넣지 않는다.

**목차**

```js
B.toc(deck, { items:[{n:'01',t:'현황 진단',p:3},{t:'업무시간 구조와 절감 여력',p:3}, …] });
```

슬라이드 번호는 마스터의 자동 번호 필드가 하단 가운데에 넣고, 우측 상단 문서 제목도 마스터가 그린다(`docTitle`). 출처는 푸터가 아니라 해당 표·도식 아래에 붙이되, **단위와 출처를 한 줄로 합친다**(§5).

## 4. 표가 주인공이다

**모든 데이터 표는 `tableNative`(PowerPoint 네이티브)를 쓴다.**

- 머리글은 **가운데 정렬**, 먹 배경에 흰 글자
- **셀 윤곽선을 그리지 않는다** — 머리글 밴드 + 교차 음영(`#FAFAFA`) + 하단 마감선
- 숫자 열은 오른쪽, 구분·주기 등 짧은 값은 가운데, 서술은 왼쪽 정렬
- 강조할 열은 `boldCol`로 지정

네이티브 표는 셀 글이 길면 **행이 자란다.** 헬퍼가 열 폭 대비 줄 수를 미리 추정해 늘어난 높이를 계산하고, 본문 벽을 넘으면 생성을 중단한다. 도식과 x좌표를 맞춰야 하는 표만 `table`(도형)로 남긴다.

## 5. 문서형 장표의 관례

- **Executive Summary**를 2쪽에 두고 KPI 6개 + 3열 요약 + 우선 과제 표로 구성
- **각주는 오독 위험이 있을 때만 단다.** 단위·기준시점·제외 조건처럼 없으면 수치를 잘못 읽는 정보에 한정한다.
- **단위와 출처는 한 줄로 합쳐** 표 바로 아래에 둔다 — `단위: 억 원 / 출처: 기관 예산요구서(’25.9)`
- **결론 밴드(`callout`)는 장(章)의 마지막 장표에만 단다.** 매 장표마다 달면 결론의 무게가 사라진다.
- 잔글씨가 한 장표에 3줄 이상이거나 결론 밴드가 본문 장표의 60%를 넘으면 점검기가 경고한다.

**도형 안에 글을 넣을 때는 `panel()`을 쓴다.** 제목은 **좌우 가운데 정렬**하고 얇은 밑줄을 둔 뒤, 본문 항목을 **박스 높이에 맞춰 줄간격을 벌려** 아래가 비지 않게 채운다. 글이 넘치면 필요한 높이를 알려주며 생성을 멈춘다.

```js
B.panel(s, { x:B.cx(0), y:2.46, w:B.cw(6), h:1.86, title:'추진 배경', items:[
  { t:'문화ODA가 법정 사무로 신설되어 ’26.11.20부터 시행', sub:'국제문화교류진흥법 제5조의2' },
  '기획예산처 재설치로 문화외교 추진체계와 재정 협의 창구가 함께 이동',
]});
```


## 6. 문안 — 사람이 쓴 문장으로

개조식은 지키되, 공문서 축약형과 명사 나열을 걷어낸다. **문장 하나에 메시지 하나, 서른 자 안팎, 판단은 근거와 함께 적는다.**

| 걷어낼 버릇 | 이렇게 쓰지 않는다 | 이렇게 쓴다 |
| --- | --- | --- |
| 명사만 늘어놓기 | 대내외 환경분석·이해관계자 니즈분석을 통한 시사점 도출 | 환경 변화와 이해관계자 요구에서 전략 시사점을 도출 |
| 공문 축약형 | 우선순위 없는 전면 착수는 지양 필요 | 전면 착수보다 상위 4개 과제에 자원을 모으는 편이 효과적 |
| `~ 시` / `~ 불가` | 게이트 미통과 시 다음 단계 진입 유보 | 게이트를 통과하지 못한 과제는 다음 단계로 넘기지 않음 |
| 뜻 없는 외래어 | 성과관리체계 고도화 및 니즈 분석 실시 | 성과관리체계를 정비하고 이해관계자 요구를 확인 |
| 근거 없는 당위 | 실효성 재점검 필요 | 현 체계로는 개정 법령의 신규 사무를 담기 어려워 재점검이 불가피 |
| 번역투·이중 피동 | 조치가 요구되어지는 것으로 판단됨 | 조치가 필요합니다 |
| 수치 없는 평가 | 예산이 대폭 증액되었음 | 국제교류 예산이 6.5억 원(23%) 늘어 증액분의 61%를 차지 |

- **헤드라인은 주장을 담은 완결문**으로 쓴다. "현황 분석"이 아니라 "절감 여력의 62%가 문서 작성 세 갈래에 몰려 있습니다".
- **표를 그대로 읽는 문장은 결론이 아니다.** 수치가 말하지 않는 함의를 적는다.
- 점검기가 `되어지`, `것으로 판단됨`, `니즈`, `제고`, `~ 시`, `지양 필요`, `대폭`, `등을 통한`을 경고로 잡는다.

## 7. QA

```bash
python3 $SK/scripts/check_layout.py deck.pptx --mode report --special <표지 번호>
python3 /mnt/skills/public/pptx/scripts/office/validate.py deck.pptx
soffice --headless --convert-to pdf deck.pptx && pdftoppm -png -r 78 deck.pdf p   # 전 장표 육안
```

점검기가 잡는 것 — A4 판형, 맑은 고딕 외 폰트(표·차트 내부까지), 8.5pt 미만, 슬라이드 밖 이탈, 본문 벽 침범(**네이티브 표는 `<a:tr h>` 합산으로 실제 높이 계산** — pptxgenjs는 표 높이를 1.0"로 고정 기재하므로 그 값을 믿으면 안 된다), 5존 앵커 이탈, 하단 30% 공백, **본문 빈 띠**(0.45" 초과 — 표를 늘려 채우라는 신호), **요소 겹침**(글자가 도형에 30% 이상 가림, **네이티브 표가 콜아웃·밴드와 겹침**), **의도하지 않은 검은 윤곽선**, 흐린 글자색, 유채색, 로고 위치·비율·로고 뒤 도형, 이모지, 차트 구조 결함.

점검기가 새로 잡는 것 — **표와 도형의 겹침**(네이티브 표는 graphicFrame이라 글자 겹침 규칙에 안 걸린다), **각주 과다**(잔글씨 3줄 이상), **결론 밴드 남용**(본문 장표 60% 초과), **문안**(이중 피동·번역투·공문 축약형).

**렌더 QA는 반드시 한다.** 밀도가 높을수록 요소가 겹치기 쉽고, 겹침은 좌표 검사로 안 걸린다.

## 8. 자주 하는 실수

- **하단이 비었는데 그냥 둠** — 점검기의 `빈 띠` 경고가 나오면 표 행 높이를 키워 6.85"까지 채운다.
- **불릿이 상자를 넘침** — `bullets()`가 필요 높이를 계산해 막는다. 높이를 키우거나 문장을 줄인다.
- `line: { width: 0 }` — pptxgenjs가 무시하고 1pt `#333333` 선을 그린다. `line: { type: 'none' }`을 쓴다.
- 표 열 폭 합계가 우측 여백 초과 — 헬퍼가 예외를 던진다. `colW` 합계는 `10.3333 − x` 이하.
- 표 아래 콜아웃·각주와 겹침 — 표 반환값 `bottom`으로 다음 요소 y를 잡는다.
- 흰 배경에 `#C7C7C7` 글자 — 최소 `#6B6B6B`.
- 밑줄을 임의 길이로 — `textW()`로 글자 폭에 맞춘다.
- **카드 제목을 왼쪽에 붙이고 글을 위로 몰아 아래를 비움** — `panel()`을 쓰면 제목이 가운데로 가고 본문이 박스를 채운다.
- **모든 표에 각주를 붙임** — 오독 위험이 있는 표에만 단다.

## 9. 파일 규칙

- 산출 파일명: `<보고서명>_v<n>.pptx`
- 동봉 자산(로고·모듈)은 수정하지 않는다 — 사본으로 작업
- 차트·표 텍스트는 라이브 텍스트로 유지한다
